/**
 * @btx/mcp-gateway — Day 1 skeleton.
 *
 * MCP server reference implementation with per-tool-call BTX service-challenge
 * admission. Companion to @btx/challenges-sdk.
 *
 * Real implementation lands Day 5-6 of the 9-day plan. This file exists to
 * make the package shape concrete + give downstream tooling something to import.
 */

export const SDK_VERSION = '0.0.1' as const;
export const STATUS = 'scaffold' as const;
