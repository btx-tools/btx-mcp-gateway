/**
 * Example tools for @btx-tools/mcp-gateway.
 *
 * These are illustrative stubs — they demonstrate the wrapper pattern at two
 * difficulty tiers (moderate + high). Real adopters should fork them or
 * write their own gated tools.
 *
 * Not exported from the package root — import explicitly:
 *   import { expensiveSearchTool, secureCalendarWriteTool } from '@btx-tools/mcp-gateway/example-tools';
 */

export { expensiveSearchTool } from './expensive-search.js';
export { secureCalendarWriteTool } from './secure-calendar-write.js';
